const giveMeSomething = (a) => {
    const myWord = "something";
    const completed = (`${myWord} ${a}.`);
    return completed;
}

const myGive = giveMeSomething("is better than nothing");
console.log(myGive);

const myGive2 = giveMeSomething("is ready for Paul.");
console.log(myGive2);

//got this no orries